[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gb-loader"
version = "0.1.0"

[project.scripts]
gb-load = "send_script:main"

[tool.setuptools]
py-modules = ["send_script"]